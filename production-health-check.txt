PRODUCTION HEALTH CHECK RESULTS NEEDED

Please run these commands on your server and share the output:

1. PM2 Status:
   sudo -u cricket-scorer pm2 status

2. Process Check:
   ps aux | grep -E "(node|cricket)" | grep -v grep

3. Port Check:
   netstat -tlnp | grep :3000

4. Nginx Status:
   systemctl status nginx --no-pager

5. Application Directory:
   ls -la /opt/cricket-scorer/
   ls -la /opt/cricket-scorer/dist/
   ls -la /opt/cricket-scorer/server/public/

6. Recent Nginx Errors:
   tail -10 /var/log/nginx/error.log

This will help identify exactly what caused the production failure.